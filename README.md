The code was meant to calculate and determine the speed of data transmission across a network by use of timestamps assigned to a pacjet of data as it is being transmitted. 
This timestamps are then used to estimate network latency (Round trip time ) and offsets (difference between the local clock and the server clock )
